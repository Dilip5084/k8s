apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  namespace: roboshop
  labels:
    project: own
    env: prod
spec:
  containers:
    - name: nginx
      image: nginx
    - name: almalinux
      image: almalinux:9
      command: ["sleep", "100"]